name: Release

on:
  push:
    tags:
      - 'v*.*.*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version tag (e.g., v0.3.0)'
        required: true
        type: string

env:
  CARGO_TERM_COLOR: always

jobs:
  build-release:
    name: Build Release
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            artifact_name: unifictl-linux-x86_64
            asset_name: unifictl-x86_64-unknown-linux-gnu.tar.gz
          - os: macos-latest
            target: x86_64-apple-darwin
            artifact_name: unifictl-macos-x86_64
            asset_name: unifictl-x86_64-apple-darwin.tar.gz
          - os: macos-latest
            target: aarch64-apple-darwin
            artifact_name: unifictl-macos-aarch64
            asset_name: unifictl-aarch64-apple-darwin.tar.gz
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            artifact_name: unifictl-windows-x86_64
            asset_name: unifictl-x86_64-pc-windows-msvc.zip

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            target/
          key: ${{ runner.os }}-cargo-${{ matrix.target }}-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-${{ matrix.target }}-
            ${{ runner.os }}-cargo-

      - name: Build release binary
        run: |
          cargo build --release --target ${{ matrix.target }}
          strip target/${{ matrix.target }}/release/unifictl || true

      - name: Create release archive (Unix)
        if: runner.os != 'Windows'
        shell: bash
        run: |
          VERSION=${GITHUB_REF#refs/tags/}
          if [ -z "$VERSION" ]; then
            VERSION=${{ github.event.inputs.version }}
          fi
          
          mkdir -p release
          cp target/${{ matrix.target }}/release/unifictl release/
          cp README.md LICENSE release/
          tar czf ${{ matrix.asset_name }} -C release .
          rm -rf release

      - name: Create release archive (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $VERSION = "${{ github.ref }}".Replace("refs/tags/", "")
          if ([string]::IsNullOrEmpty($VERSION)) {
            $VERSION = "${{ github.event.inputs.version }}"
          }
          
          New-Item -ItemType Directory -Force -Path release | Out-Null
          Copy-Item "target/${{ matrix.target }}/release/unifictl.exe" "release/unifictl.exe"
          Copy-Item README.md "release/"
          Copy-Item LICENSE "release/"
          Compress-Archive -Path release/* -DestinationPath "${{ matrix.asset_name }}" -Force
          Remove-Item -Recurse -Force release

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: ${{ matrix.asset_name }}
          retention-days: 30

  build-deb-packages:
    name: Build DEB Packages
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        arch: [amd64, arm64]
        include:
          - arch: amd64
            target: x86_64-unknown-linux-gnu
          - arch: arm64
            target: aarch64-unknown-linux-gnu

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install cargo-deb
        run: cargo install cargo-deb

      - name: Install cross-compilation dependencies (arm64)
        if: matrix.arch == 'arm64'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            gcc-aarch64-linux-gnu \
            g++-aarch64-linux-gnu \
            libc6-dev-arm64-cross

      - name: Build release binary (x86_64)
        if: matrix.arch == 'amd64'
        run: |
          cargo build --release --target ${{ matrix.target }} --verbose
          strip target/${{ matrix.target }}/release/unifictl || true
        env:
          CARGO_BUILD_TARGET: ${{ matrix.target }}
          OPENSSL_STATIC: 1
          OPENSSL_VENDORED: 1

      - name: Build release binary (aarch64)
        if: matrix.arch == 'arm64'
        run: |
          CARGO_BUILD_JOBS=1 cargo build --release --target ${{ matrix.target }} --verbose
          strip target/${{ matrix.target }}/release/unifictl || true
        env:
          CARGO_BUILD_TARGET: ${{ matrix.target }}
          OPENSSL_STATIC: 1
          OPENSSL_VENDORED: 1
          CC_aarch64_unknown_linux_gnu: aarch64-linux-gnu-gcc
          CXX_aarch64_unknown_linux_gnu: aarch64-linux-gnu-g++
          AR_aarch64_unknown_linux_gnu: aarch64-linux-gnu-ar
          CARGO_TARGET_AARCH64_UNKNOWN_LINUX_GNU_LINKER: aarch64-linux-gnu-gcc
          PKG_CONFIG_ALLOW_CROSS: 1
          OPENSSL_BUILD_JOBS: 1
          MAKEFLAGS: "-j1"

      - name: Determine version
        id: version
        shell: bash
        run: |
          if [ "${{ github.event_name }}" == "workflow_dispatch" ]; then
            VERSION="${{ github.event.inputs.version }}"
          else
            VERSION=${GITHUB_REF#refs/tags/}
          fi
          VERSION=${VERSION#v}
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Version: $VERSION"

      - name: Build DEB package
        run: |
          cargo deb --target ${{ matrix.target }} --no-build
          VERSION=${{ steps.version.outputs.version }}
          mv target/${{ matrix.target }}/debian/unifictl_*.deb \
             unifictl_${VERSION}_${{ matrix.arch }}.deb

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: unifictl-deb-${{ matrix.arch }}
          path: unifictl_*_${{ matrix.arch }}.deb
          retention-days: 30

  build-arch-packages:
    name: Build Arch Linux Packages
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        arch: [x86_64, aarch64]
        include:
          - arch: x86_64
            target: x86_64-unknown-linux-gnu
          - arch: aarch64
            target: aarch64-unknown-linux-gnu

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install makepkg dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y fakeroot binutils zstd

      - name: Install cross-compilation dependencies
        if: matrix.arch == 'aarch64'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            gcc-aarch64-linux-gnu \
            g++-aarch64-linux-gnu \
            libc6-dev-arm64-cross

      - name: Build release binary (x86_64)
        if: matrix.arch == 'x86_64'
        run: |
          cargo build --release --target ${{ matrix.target }} --verbose
          strip target/${{ matrix.target }}/release/unifictl || true
        env:
          CARGO_BUILD_TARGET: ${{ matrix.target }}
          OPENSSL_STATIC: 1
          OPENSSL_VENDORED: 1

      - name: Build release binary (aarch64)
        if: matrix.arch == 'aarch64'
        run: |
          # Use single job to avoid OpenSSL make jobserver issues
          CARGO_BUILD_JOBS=1 cargo build --release --target ${{ matrix.target }} --verbose
          strip target/${{ matrix.target }}/release/unifictl || true
        env:
          CARGO_BUILD_TARGET: ${{ matrix.target }}
          OPENSSL_STATIC: 1
          OPENSSL_VENDORED: 1
          CC_aarch64_unknown_linux_gnu: aarch64-linux-gnu-gcc
          CXX_aarch64_unknown_linux_gnu: aarch64-linux-gnu-g++
          AR_aarch64_unknown_linux_gnu: aarch64-linux-gnu-ar
          CARGO_TARGET_AARCH64_UNKNOWN_LINUX_GNU_LINKER: aarch64-linux-gnu-gcc
          PKG_CONFIG_ALLOW_CROSS: 1
          # Force single-threaded OpenSSL build
          OPENSSL_BUILD_JOBS: 1
          MAKEFLAGS: "-j1"

      - name: Determine version
        id: version
        shell: bash
        run: |
          if [ "${{ github.event_name }}" == "workflow_dispatch" ]; then
            VERSION="${{ github.event.inputs.version }}"
          else
            VERSION=${GITHUB_REF#refs/tags/}
          fi
          VERSION=${VERSION#v}
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Version: $VERSION"

      - name: Update PKGBUILD
        run: |
          sed -i "s/^pkgver=.*/pkgver=${{ steps.version.outputs.version }}/" packaging/arch/PKGBUILD
          sed -i "s/^pkgrel=.*/pkgrel=1/" packaging/arch/PKGBUILD

      - name: Create package directory
        run: |
          mkdir -p pkg/usr/bin
          mkdir -p pkg/usr/share/doc/unifictl
          mkdir -p pkg/usr/share/licenses/unifictl

      - name: Install files
        run: |
          install -Dm755 target/${{ matrix.target }}/release/unifictl pkg/usr/bin/unifictl
          install -Dm644 README.md pkg/usr/share/doc/unifictl/README.md
          install -Dm644 LICENSE pkg/usr/share/licenses/unifictl/LICENSE

      - name: Create .PKGINFO
        run: |
          cat > pkg/.PKGINFO <<EOF
          pkgname = unifictl
          pkgver = ${{ steps.version.outputs.version }}-1
          pkgdesc = CLI for UniFi Site Manager (API v1/EA)
          url = https://github.com/nachtschatt3n/unifictl
          builddate = $(date +%s)
          packager = GitHub Actions <actions@github.com>
          size = $(du -sb pkg | cut -f1)
          arch = ${{ matrix.arch }}
          license = GPL-3.0
          depend = openssl
          depend = glibc
          EOF

      - name: Create .MTREE
        run: |
          cd pkg
          find . -type f -o -type d | sort | sed 's|^\./||' | while read line; do
            if [ -d "$line" ]; then
              echo "/$line type=dir"
            else
              MODE=$(stat -c %a "$line" 2>/dev/null || echo 644)
              SIZE=$(stat -c %s "$line" 2>/dev/null || echo 0)
              echo "/$line type=file mode=$MODE size=$SIZE"
            fi
          done > .MTREE
          cd ..

      - name: Create package archive
        run: |
          cd pkg
          tar cf - .PKGINFO .MTREE usr/ | zstd -o ../unifictl-${{ steps.version.outputs.version }}-1-${{ matrix.arch }}.pkg.tar.zst
          cd ..
          mv unifictl-${{ steps.version.outputs.version }}-1-${{ matrix.arch }}.pkg.tar.zst unifictl-${{ matrix.arch }}.pkg.tar.zst

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: unifictl-arch-${{ matrix.arch }}
          path: unifictl-${{ matrix.arch }}.pkg.tar.zst
          retention-days: 30

  publish-crates-io:
    name: Publish to crates.io
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/') || github.event_name == 'workflow_dispatch'
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Determine version
        id: version
        shell: bash
        run: |
          if [ "${{ github.event_name }}" == "workflow_dispatch" ]; then
            VERSION="${{ github.event.inputs.version }}"
          else
            VERSION=${GITHUB_REF#refs/tags/}
          fi
          VERSION=${VERSION#v}
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Version: $VERSION"

      - name: Verify version matches Cargo.toml
        run: |
          CARGO_VERSION=$(grep '^version =' Cargo.toml | cut -d'"' -f2)
          TAG_VERSION="${{ steps.version.outputs.version }}"
          # Remove 'v' prefix from tag version if present for comparison
          TAG_VERSION=${TAG_VERSION#v}
          if [ "$CARGO_VERSION" != "$TAG_VERSION" ]; then
            echo "Error: Cargo.toml version ($CARGO_VERSION) doesn't match tag version ($TAG_VERSION)"
            exit 1
          fi
          echo "✓ Version matches: $CARGO_VERSION"

      - name: Run tests
        run: cargo test --all-targets

      - name: Check formatting
        run: cargo fmt --all -- --check

      - name: Run Clippy
        run: cargo clippy --all-targets --all-features -- -W clippy::all

      - name: Dry-run publish
        run: cargo publish --dry-run --allow-dirty

      - name: Publish to crates.io
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CRATES_IO_TOKEN }}
        run: |
          if [ -z "$CARGO_REGISTRY_TOKEN" ]; then
            echo "⚠️  CRATES_IO_TOKEN secret not set. Skipping crates.io publish."
            echo "   To enable automated publishing, add CRATES_IO_TOKEN to repository secrets."
            exit 0
          fi
          cargo publish --allow-dirty

  create-release:
    name: Create Release
    needs: [build-release, build-arch-packages, build-deb-packages]
    runs-on: ubuntu-latest
    if: always() && (startsWith(github.ref, 'refs/tags/') || github.event_name == 'workflow_dispatch')
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
        continue-on-error: true

      - name: Determine version
        id: version
        run: |
          if [ "${{ github.event_name }}" == "workflow_dispatch" ]; then
            VERSION="${{ github.event.inputs.version }}"
          else
            VERSION=${GITHUB_REF#refs/tags/}
          fi
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Version: $VERSION"

      - name: Generate checksums
        shell: bash
        run: |
          if [ -d artifacts ] && [ "$(ls -A artifacts 2>/dev/null)" ]; then
            cd artifacts
            find . -type f \( -name "*.tar.gz" -o -name "*.zip" -o -name "*.deb" -o -name "*.pkg.tar.zst" \) -exec sha256sum {} \; > checksums.txt || touch checksums.txt
          else
            echo "No artifacts found, creating empty checksums file"
            mkdir -p artifacts
            touch artifacts/checksums.txt
          fi

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ steps.version.outputs.version }}
          name: Release ${{ steps.version.outputs.version }}
          generate_release_notes: false
          make_latest: true
          body: |
            ## Release ${{ steps.version.outputs.version }}

            ### Downloads

            **Linux:**
            - [x86_64](https://github.com/${{ github.repository }}/releases/download/${{ steps.version.outputs.version }}/unifictl-x86_64-unknown-linux-gnu.tar.gz)

            **macOS:**
            - [x86_64](https://github.com/${{ github.repository }}/releases/download/${{ steps.version.outputs.version }}/unifictl-x86_64-apple-darwin.tar.gz)
            - [aarch64 (Apple Silicon)](https://github.com/${{ github.repository }}/releases/download/${{ steps.version.outputs.version }}/unifictl-aarch64-apple-darwin.tar.gz)
            
            **Windows:**
            - [x86_64](https://github.com/${{ github.repository }}/releases/download/${{ steps.version.outputs.version }}/unifictl-x86_64-pc-windows-msvc.zip)

            ### Installation

            **Linux:**
            ```bash
            tar xzf unifictl-*-unknown-linux-gnu.tar.gz
            sudo mv unifictl /usr/local/bin/
            ```

            **macOS:**
            ```bash
            tar xzf unifictl-*-apple-darwin.tar.gz
            sudo mv unifictl /usr/local/bin/
            ```
            
            **Windows:**
            ```powershell
            Expand-Archive unifictl-*-windows-msvc.zip
            # Add to PATH or copy unifictl.exe to desired location
            ```

            **Debian/Ubuntu (.deb):**
            - [amd64](https://github.com/${{ github.repository }}/releases/download/${{ steps.version.outputs.version }}/unifictl_${{ steps.version.outputs.version }}_amd64.deb)
            - [arm64](https://github.com/${{ github.repository }}/releases/download/${{ steps.version.outputs.version }}/unifictl_${{ steps.version.outputs.version }}_arm64.deb)
            
            Install with:
            ```bash
            sudo dpkg -i unifictl_*_amd64.deb
            ```

            **Arch Linux:**
            - [x86_64](https://github.com/${{ github.repository }}/releases/download/${{ steps.version.outputs.version }}/unifictl-x86_64.pkg.tar.zst)
            - [aarch64](https://github.com/${{ github.repository }}/releases/download/${{ steps.version.outputs.version }}/unifictl-aarch64.pkg.tar.zst)
            
            Install with:
            ```bash
            sudo pacman -U unifictl-*.pkg.tar.zst
            ```

            ### Checksums
            ```
            ${{ github.workspace }}/artifacts/checksums.txt
            ```

            ### Install from crates.io

            ```bash
            cargo install unifictl
            ```
          files: |
            artifacts/**/*.tar.gz
            artifacts/**/*.zip
            artifacts/**/*.deb
            artifacts/**/*.pkg.tar.zst
            artifacts/**/checksums.txt
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
